# ==============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# ==============================================================================
#
# Copy this file to .env.development and fill in your actual values
# cp .env.development.example .env.development
# nano .env.development
#
# ⚠️ SECURITY WARNING: Never commit .env.development file to version control!
#
# ✅ DEVELOPMENT MODE:
# - Uses separate ports (3001 for backend, 4001 for frontend)
# - Uses separate database (recommended: khoahocgiare_dev)
# - Uses separate Redis database (REDIS_DB=1)
# - Does NOT affect production server
#
# ==============================================================================

# Server Environment
NODE_ENV=development

# Server Port (Development uses 3001, Production uses 3000)
PORT=3001

# CORS Configuration for Development
CORS_ORIGIN=http://localhost:4001,http://localhost:4000

# Frontend URL (Development)
FRONTEND_URL=http://localhost:4001

# Trust Proxy (usually false in development)
TRUST_PROXY=false

# ==============================================================================
# DATABASE CONFIGURATION
# ⚠️ IMPORTANT: Use a SEPARATE database for development!
# ==============================================================================

DB_HOST=localhost
DB_NAME=khoahocgiare_dev
DB_USER=root
DB_PASSWORD=your_password_here

# ==============================================================================
# REDIS CONFIGURATION
# ✅ Development uses Redis database 1, Production uses database 0
# ==============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# ==============================================================================
# LOGGING CONFIGURATION
# ==============================================================================

LOG_LEVEL=debug

# WebSocket Path
WEBSOCKET_PATH=/socket.io

# Session Secret (use different secret for development)
SESSION_SECRET=dev-secret-key-change-in-production

# JWT Secret (use different secret for development)
JWT_SECRET=dev-jwt-secret-change-in-production
